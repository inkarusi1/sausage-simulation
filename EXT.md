# PositionBasedDynamics 扩展功能说明

本文记录“按 Space 暂停后编辑场景物体”的需求、原仓库支持情况、当前课程新增 demo 的实现状态，以及后续可继续扩展的方向。

相关文档：

- `REF.md`：物理和源码设计参考，包含新增 demo 可复用设计。
- `OP.md`：键鼠操作和快捷键冲突参考，包含 `SausageRodEditorDemo` 暂停编辑键鼠操作。
- `EXP.md`：课程项目必要设计与调试经验。
- `README.md`：当前文档入口。

## 需求目标

希望项目运行后按 Space 暂停动画时，尽量支持：

| 目标 | 期望行为 |
| --- | --- |
| 鼠标拖动物品 | 暂停后直接改变物体位置，而不是给速度或力。 |
| 缩放物品 | 调整物体尺寸，并保持视觉和碰撞尽量一致。 |
| 旋转物品 | 暂停后改变物体朝向，恢复仿真后不产生异常速度。 |
| 新增物品 | 在场景中加入新物体，并让它可见、可选择、可碰撞。 |
| 删除物品 | 移除指定物体，避免破坏已有约束、碰撞对象和索引。 |

## 原仓库现成能力结论

当前原仓库 demo 更像仿真 viewer，不是完整场景编辑器。按 Space 暂停后，原生能力不能完整满足暂停编辑。

| 目标 | 原仓库暂停状态支持情况 | 主要原因 |
| --- | --- | --- |
| 鼠标拖动物品 | 不满足编辑器式拖动 | 现有中键拖动主要给选中动态刚体/粒子加速度或速度；暂停时仿真步进 return，不会积分位置。 |
| 缩放物品 | 不支持运行时物体缩放 | `scale` 多在 JSON 加载或 `initBody()` 时使用；GUI Scale 是界面缩放。 |
| 旋转物品 | 不支持暂停旋转 gizmo | Alt+左键旋转的是相机；物体旋转来自仿真或代码。 |
| 新增物品 | 不支持现成 UI | 需要同步模型数组、碰撞对象、约束、ID 映射和选择状态。 |
| 删除物品 | 不支持安全单个删除 | 刚体下标、碰撞对象、关节/约束引用都可能被影响。 |

关键源码依据：

- `Demos/SceneLoaderDemo/SceneLoaderDemo.cpp`：暂停时 `timeStep()` 直接 return。
- `Demos/Common/DemoBase.cpp`：现有选择和拖动更偏“施力/改速度”，不是直接 transform 编辑。
- `Demos/Visualization/MiniGL.cpp`：Ctrl/Shift/Alt + 左键主要是相机平移/旋转。
- `Demos/Common/Simulator_GUI_imgui.cpp`：没有物体新增、删除、旋转、缩放面板。

## 当前已落地实现

新增 demo：

- 源码：`Demos/SausageRodEditorDemo/main.cpp`
- 可执行文件：`bin/SausageRodEditorDemo.exe`
- CMake 目标：`SausageRodEditorDemo`

该 demo 基于 `SausageRodCourseDemo`，增加了暂停编辑层。它不是修改全仓库通用 `DemoBase`，而是在课程 demo 内部实现局部编辑能力，以降低对原仓库结构的破坏。

### 已实现功能

| 功能 | 当前状态 | 说明 |
| --- | --- | --- |
| 暂停后选择物体 | 已实现 | Space 暂停后，左键按下会选择最近可编辑对象。 |
| 暂停后鼠标拖动物体 | 已实现 | 左键拖动会直接平移选中对象。 |
| 键盘平移 | 已实现 | `i/k/j/l/u/o` 控制 y/x/z 方向移动。 |
| 键盘旋转 | 已实现 | `z/x/c` 分别绕全局 Z/X/Y 轴旋转选中对象。 |
| 缩放物体 | 已实现 | `[` / `]` 支持香肠、原场景对象和编辑器新增对象的等比缩放；盒体/球体更新解析碰撞，SDF 对象同步视觉网格和 SDF 缩放值。 |
| 盒体局部拉伸 | 已实现 | `f/g/h` 沿局部前/左/上方向拉伸盒体，并同步 box 碰撞。 |
| 新增物体 | 已实现 | `1/2/3/4/5/6` 选择盒体/球体/锥体/圆环/弯曲滑轨/直滑轨，`n` 新增当前形状。 |
| 删除物体 | 部分实现 | `d` 只删除编辑器新增对象，不删除原始场景对象。 |
| 碰撞同步 | 已实现 | 新增对象会同时创建刚体和解析碰撞对象；滑轨和锥体使用程序网格 + SDF。 |
| 固定/动态切换 | 已实现 | 暂停后选中编辑器新增对象，按 `t` 在 fixed/dynamic 间切换。 |
| 保存/加载编辑场景 | 已实现 | `p` 保存、`b` 加载，文件为 `bin/SausageRodEditorScene.json`；用于恢复上次新增、删除、移动、旋转、缩放后的编辑状态。 |
| 暂停编辑高亮 | 已实现 | 暂停时选中对象附近会绘制绿色提示标记。 |

完整键鼠操作见 `OP.md` 的 `SausageRodEditorDemo 暂停编辑操作` 小节。

### 当前可编辑对象

| 对象 | 可平移 | 可旋转 | 可缩放 | 可删除 |
| --- | --- | --- | --- | --- |
| 香肠 | 是 | 是 | 是 | 否 |
| 平台 | 是 | 是 | 是 | 否 |
| 第一个圆环 | 是 | 是 | 是 | 否 |
| 第二个圆环 | 是 | 是 | 是 | 否 |
| 半管滑轨 | 是 | 是 | 是 | 否 |
| 编辑器新增盒体 | 是 | 是 | 是 | 是 |
| 编辑器新增球体 | 是 | 是 | 是 | 是 |
| 编辑器新增锥体 | 是 | 是 | 是 | 是 |
| 编辑器新增圆环 | 是 | 是 | 是 | 是 |
| 编辑器新增弯曲/直滑轨 | 是 | 是 | 是 | 是 |

## 当前实现设计

### 编辑对象表

`SausageRodEditorDemo` 中新增 `EditableRigidBody` 表，记录：

- `bodyIndex`
- 显示名称
- 形状类型
- box scale
- sphere radius
- 是否可缩放
- 是否可删除
- 摩擦和回弹
- 是否 active

香肠不是原仓库 `RigidBody`，因此作为特殊对象单独处理。

### 暂停模式输入路由

新增编辑器只在暂停状态处理输入：

- 未暂停时，鼠标/键盘尽量交回原 viewer 和仿真逻辑。
- 暂停时，`MiniGL::addMousePressFunc()` 和 `MiniGL::addMouseMoveFunc()` 抢占无修饰左键拖动。
- 快捷键避开 `OP.md` 中高风险键：Space、`r`、`w`、ESC、方向键、中键和修饰键左键组合。数字键当前用于编辑器新建形状选择。

### 刚体 transform 同步

暂停编辑静态刚体时，不能只改 `setPosition()` 或 `setRotation()`。当前实现统一同步：

1. 当前、上一帧、旧位置。
2. 当前、上一帧、旧旋转。
3. 速度和角速度清零。
4. rotation matrix。
5. `updateInverseTransformation()`。
6. `getGeometry().updateMeshTransformation(...)`。

这点很重要：质量为 0 的静态刚体不会通过 `rotationUpdated()` 自动完成全部更新。

### 新增物体

数字键选择待新增形状，`n` 新增当前形状。盒体/球体/锥体/滑轨的流程：

1. 加载或程序生成对应网格。
2. 调用 demo 内部 `addBody(...)` 创建静态刚体。
3. 调用 `addCollisionBox(...)`、`addCollisionSphere(...)` 或 SDF 注册函数创建碰撞对象。
4. 登记到 `EditableRigidBody` 表。
5. 自动选中新对象。

### 删除新增对象

当前只允许删除编辑器新增对象。删除采用“停用并移动到远处”的方式：

- 不从 `RigidBodyVector` 中 erase。
- 不重排碰撞对象。
- 不影响原始场景对象索引。

这样比 live delete 更稳，因为原仓库没有通用安全删除单个刚体并同步所有约束/碰撞引用的 API。

### 缩放与拉伸

当前对所有可编辑对象开放暂停时等比缩放：

1. 更新 `EditableRigidBody::boxScale`。
2. 重新 `initBody()` cube 网格。
3. 同步刚体 transform。
4. 更新 `DistanceFieldCollisionBox::m_box`。

球体缩放会更新 `EditableRigidBody::sphereRadius`，重新初始化 sphere 网格，并更新 `DistanceFieldCollisionSphere::m_radius`。

盒体还支持 `f/g/h` 局部前/左/上方向拉伸。实现时会按盒体当前旋转计算局部轴的世界方向，改变对应尺寸，并把中心移动半个增量，保持从一侧拉出的效果。

香肠缩放会同步中心线点位置、`restLocal`、长度和半径，并清零速度，避免恢复仿真后一帧突然弹开。

圆环、锥体和半管滑轨属于复杂 SDF 网格。当前只开放等比缩放，做法是同步缩放视觉网格、编辑器包围半径和 `CubicSDFCollisionObject::m_scale`；如果后续要做非等比缩放或改变滑轨/圆环参数，仍应重建程序网格和 SDF。

### 保存与加载

当前 demo 提供轻量编辑场景存档，不直接复用原仓库 `SceneLoaderDemo` 的 JSON 格式。

- `p`：保存到 `bin/SausageRodEditorScene.json`。
- `b`：从 `bin/SausageRodEditorScene.json` 加载。
- 保存内容包括柔软度、香肠长度/半径和中心线状态、原场景可编辑物体的 transform/缩放、编辑器新增物体的类型/动态状态/速度、删除后的 inactive 状态。
- 加载时会先重建基础课程场景，再按保存文件恢复对象状态，因此适合“下次继续编辑”，但不是通用 PBD 场景导出器。

## 与原仓库功能的区别

| 项目 | 原仓库功能 | 新增课程 demo 功能 |
| --- | --- | --- |
| 场景构建 | 多数通过 JSON + `SceneLoaderDemo` | 手写 C++ 程序场景 |
| 暂停逻辑 | 暂停后仿真步进 return | 暂停后额外启用编辑输入 |
| 拖动 | 运行时给动态对象速度/力 | 暂停时直接改 transform |
| 新增/删除 | 无通用 UI | 在局部 demo 中新增/停用盒体、球体、锥体和滑轨 |
| 缩放 | 加载时 scale | 暂停时支持香肠、box/sphere 和复杂 SDF 的等比缩放 |
| 碰撞同步 | SceneLoader 创建碰撞对象 | 新增对象手动同步刚体和解析碰撞对象 |

## 仍未实现的能力

这些功能还没有做成完整编辑器：

- 完整网格拾取，目前是近似选择最近对象。
- ImGui 属性面板，例如 position/rotation/scale 数值输入。
- 对圆环/滑轨等复杂 SDF 对象的非等比缩放和 SDF 重建。
- 对原始场景对象的安全删除。
- 对布料、四面体软体、关节、马达的暂停编辑。
- 撤销/重做、复制、对象层级管理。

## 后续扩展建议

### 短期

1. 给 `SausageRodEditorDemo` 加 ImGui 面板，显示当前选中对象名称、位置、旋转和可用操作。
2. 增加精确选取方式，例如射线拾取或屏幕空间投影距离。
3. 保存/加载编辑状态已使用轻量 JSON 落地；若后续要和原仓库 SceneLoader 互通，需要另做格式映射。

### 中期

1. 对复杂 SDF 对象实现“重建式缩放”：修改参数，重新生成网格和 SDF。
2. 把编辑器对象表抽成 `Demos/Common/SceneEditor.*`，供其他 demo 复用。
3. 为删除功能实现重建式流程，避免 live erase 造成索引问题。

### 长期

1. 与 `SceneLoader::SceneData` 对接，让编辑结果可以回写为场景文件。
2. 支持布料/软体粒子集合 transform。
3. 支持关节、马达、约束对象的可视化编辑。

## 重要风险提醒

- 暂停编辑后恢复仿真，如果 old/last position 没同步，速度更新可能突然变大。
- 直接缩放网格但不更新碰撞体，会出现视觉和碰撞错位。
- 复杂 SDF 对象只改视觉缩放是不够的，距离场也要同步重建或缩放。
- 删除原始对象会影响刚体下标、碰撞对象和约束引用，不能简单 erase。
- 新增物体如果没有登记碰撞对象，会出现“看得见但碰不到”。
