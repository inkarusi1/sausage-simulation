# SausagePhysicsCourseDemo 设计与调试经验

本文只记录本次项目中可复用的必要经验，便于后续继续调试香肠、圆环、滑轨和平台场景。

## 1. 先用原仓库物理路径，不要手写速度脚本

这个项目已经实现了重力、刚体状态、接触冲量、摩擦、速度约束和四面体软体约束。新 Demo 应尽量接入原有 `SimulationModel`、`TimeStepController`、`CubicSDFCollisionDetection`、`TetModel` 和 `RigidBody`，不要用手动改速度或路径动画模拟物理现象。

重力由 `Simulation::m_gravitation` 控制，默认是 `(0, -9.81, 0)`。粒子和动态刚体在 `TimeStep::clearAccelerations()`/时间步流程中受重力影响。

## 2. 香肠不适合用刚体弹簧串作为最终方案

最早的“小球刚体 + 弹簧链”能快速做出可调柔软度，但有几个根本问题：

- 视觉半径和碰撞半径容易不一致，导致看起来穿模。
- 高柔软度时小球会被拉开，视觉上像分段香肠。
- 0% 刚体和其他柔软度如果走两套逻辑，容易出现切换闪现或行为不连续。

更合适的课程 Demo 方案是把香肠做成 `TetModel` 四面体实体：所有柔软度都走同一套粒子实体、体积约束、距离约束和碰撞流程，只改变约束刚度。

## 3. 软硬控制要考虑 PBD 迭代累积

PBD 中一次迭代的刚度不等于最终肉眼看到的有效刚度。若每帧有多次位置迭代，即使单次刚度看似较小，也会累积成很硬的效果。

因此柔软度映射应先设计“目标有效刚度”，再换算成单次迭代刚度：

```cpp
perIteration = 1 - pow(1 - effective, 1.0 / iterations);
```

经验设置：

- 0%：全局 Shape Matching 强，距离/体积约束强，近似刚体。
- 10%：仍应非常硬，只允许轻微形变。
- 100%：全局 Shape Matching 基本消失，距离和体积约束明显放松，才会接近软塌效果。

## 4. Shape Matching 适合做“近似刚体保形”，但不能一直太强

全局 `ShapeMatchingConstraint` 很适合让 0% 和低柔软度保持整体形状，避免硬香肠被拉散。但如果 100% 时仍保留较强 Shape Matching，香肠会像果冻棒而不是史莱姆。

建议把 Shape Matching 独立于普通实体刚度控制：

- 低柔软度：Shape Matching 强。
- 高柔软度：Shape Matching 快速衰减。
- 100%：Shape Matching 接近 0。

## 5. 自定义滑轨和圆环应使用视觉一致的 SDF 碰撞

滑轨和圆环如果使用简单辅助碰撞片，容易出现“看起来还有空隙但已经碰撞”或“视觉穿模但碰撞没反应”。更好的方法是程序生成实际视觉网格，并直接从该网格生成 `CubicSDF` 碰撞体。

本 Demo 中：

- 滑轨使用弯曲半管网格。
- 圆环使用程序化 torus 网格。
- 两者都注册为静态刚体，并使用 SDF 碰撞。

## 6. TetModel 与刚体 SDF 的碰撞本质是粒子采样

本项目里 `TetModel` 与刚体 SDF 障碍物碰撞时，核心是检测 TetModel 顶点/粒子是否进入障碍物 SDF。它不是连续三角面碰撞。

因此低柔软度时更容易穿模，原因是香肠几乎不变形，像硬胶囊高速撞薄壁；如果表面粒子采样不够密，粒子之间的面片可能跨过圆环或滑轨边缘。

必要调试手段：

- 增加香肠轴向和环向采样密度。
- 提高低柔软度时的 `contactTolerance`。
- 缩小单次物理 step，让碰撞检测更频繁。
- 若仍穿模，需要新增专门的碰撞采样壳或代理碰撞层，而不是继续只调刚度。

## 7. 尖锐 SDF 锥尖容易造成“吸附”

在 `SausageRodEditorDemo` 里，香肠和 SDF 障碍物的接触仍是中心线/采样点投影式处理，不是完整连续体接触。如果程序生成的锥体使用数学尖顶，锥尖附近的 SDF 法线变化很剧烈；采样点一旦贴近尖端，法线修正、切向摩擦和速度阻尼会叠加成“被锥尖吸住”的效果。

这不是单纯把摩擦调小就能完全解决的问题。摩擦过大会让吸附更明显，但尖锐几何本身也会制造不稳定接触方向。

当前处理经验：

- 新增锥体不要使用零半径尖顶，改成很小的平顶/截锥，让 SDF 顶端有可解析的面。
- 编辑器新增 SDF 物体接触香肠时使用更低的摩擦和回弹，避免切向速度被过度吃掉。
- 在没有实现 rod-rigid 双向冲量前，动态新增物体不要继续作为香肠的静态障碍参与采样修正，否则容易出现香肠或物体被异常打飞。
- 如果后续要做真正尖锥，建议单独增加圆角/倒角网格，或给锥尖附近的接触法线做稳定化，而不是只依赖高分辨率 SDF。

## 8. 下落速度慢通常不是重力错，而是每帧推进时间太小

`TimeManager::setTimeStepSize(h)` 决定一次 `step()` 推进的仿真时间。`NUM_SUB_STEPS` 只是把这个 `h` 拆成更小内部积分步，增强稳定性，并不会增加总仿真时间。

视觉下落速度约等于：

```text
每帧仿真时间 = timeStepSize * NUM_STEPS_PER_RENDER
```

如果下落太慢，应优先检查这个乘积，而不是直接把重力改大。

## 9. 性能主要受每帧求解次数和碰撞检测次数影响

卡顿时重点看：

```text
每帧位置约束工作量 ~= NUM_STEPS_PER_RENDER * NUM_SUB_STEPS * MAX_ITERATIONS
```

再加上每个 `step()` 都会进行碰撞检测，SDF 和高密度 TetModel 会进一步增加开销。

调试经验：

- 为了保持下落速度，可让 `timeStepSize * NUM_STEPS_PER_RENDER` 不变。
- 为了减少穿模，可减小 `timeStepSize` 并增加 `NUM_STEPS_PER_RENDER`，使碰撞检测更频繁。
- 为了避免卡顿，可降低 `NUM_SUB_STEPS` 或 `MAX_ITERATIONS`，但会牺牲稳定性和抗穿模能力。

## 10. 摩擦系数按接触双方组合，滑轨要单独调光滑

在当前碰撞实现中，摩擦大致按接触双方相加组合。若香肠自身摩擦较大，即使滑轨摩擦很小，也会显得滑轨有阻力。

本 Demo 的经验：

- 滑轨摩擦设为接近 0，使其足够光滑。
- 香肠自身摩擦不要过大，否则所有接触都会偏粘。
- 平台可以保持高摩擦，让香肠落到平台后明显减速。

## 11. 编译调试注意事项

如果编译时报：

```text
LINK : fatal error LNK1104: cannot open ... SausagePhysicsCourseDemo.exe
```

通常是 Demo 窗口仍在运行，占用了 exe。关闭正在运行的 `SausagePhysicsCourseDemo.exe` 后重新编译即可。

构建过程可能自动修改 `Utils/Version.h` 和 `build-vs2` 下的 `.obj/.tlog` 文件，它们通常是构建产物或版本元数据，不属于核心源码设计改动。
